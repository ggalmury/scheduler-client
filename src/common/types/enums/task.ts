export enum TaskPrivacy {
  PUBLIC = "public",
  PRIVATE = "private",
  RELEVANT = "relevant",
}

export enum TaskColor {
  BASIC = "#f1d39d",
  WORK = "#9799cd",
  MEETING = "#eab3b6",
  PERSONAL = "#ff9cadc1",
}

export enum TaskType {
  BASIC = "basic",
  WORK = "work",
  MEETING = "meeting",
  PERSONAL = "personal",
}

export enum Meridiem {
  AM = "am",
  PM = "pm",
}
